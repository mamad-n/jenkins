pipeline {
    agent any

    stages {
        stage ('test') {

            when {
                anyOf {
                    branch 'main'
                    branch 'feature'
                }
            }

            steps {
                echo "Hello, nice to meet you."
            }
        }
    }
}