node {
		checkout scm

        stage('Build') {
        sh 'echo from build'

        stage ('inner build'){
            sh 'echo from innner build ---------------> nemidonam'
        }

        stage ('inner test'){
            sh 'echo from inner test'
        }
    }
}

