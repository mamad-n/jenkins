node {
		checkout scm

        stage('Build') {
        sh 'echo from build'

        stage ('inner build'){
            checkout scm
            sh 'echo from innner build'
        }

        stage ('inner test'){
            sh 'echo from inner test'
        }
    }
}

